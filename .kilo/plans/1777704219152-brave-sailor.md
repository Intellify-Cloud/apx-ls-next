# Plan: Fix Site Issues

## Issues Found

1. **lib/data.ts** - `about.title` is "Unlocking potential through people development" (sentence case needed per user request). The `about` section uses keys `top-text` and `bottom-text` which are referenced from components but need verification.
2. **app/page.tsx** - Missing `AboutSection` import (component exists but not imported/used).
3. **app/globals.css** - Animation duration is 100s (was changed from 50s), should be 50s per earlier work.
4. **components/sections/ScarfSection.tsx** - Layout issues: max-w-[1200px] instead of consistent max-w-[800px], references `scarf.sectionTitle` which doesn't exist in data, benefits list not centered properly, button not centered.
5. **components/sections/ThreeRowTTextM3CardsBText.tsx** - References `siteContent.about["top-text"]` and `siteContent.about["bottom-text"]` which match the data structure. Uses `max-w-4xl` instead of `max-w-[800px]`.

## Steps

### 1. Fix lib/data.ts - Change about.title to sentence case
- Line 107: Change `"Unlocking potential through people development"` → `"Unlocking Potential Through People Development"` (title case as per design system headings)

### 2. Fix app/page.tsx - Add missing AboutSection
- Import AboutSection and add it to the page layout

### 3. Fix app/globals.css - Restore animation duration
- Change `animation: scroll 100s linear infinite;` → `animation: scroll 50s linear infinite;`

### 4. Fix components/sections/ScarfSection.tsx - Layout and data references
- Change `max-w-[1200px]` → `max-w-[800px]`
- Remove reference to `scarf.sectionTitle` (doesn't exist in data) - use `scarf.description` as the label instead
- Fix benefits list layout - center it properly
- Center the button using flex container
- Remove unused `label-caps` class reference if not defined

### 5. Fix components/sections/ThreeRowTTextM3CardsBText.tsx - Consistent width
- Change `max-w-4xl` → `max-w-[800px]` for consistency with other sections
